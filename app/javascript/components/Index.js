// the index route

import React from 'react'
import PropTypes from 'prop-types'

const Index = props => {
  return(
    <div className="Index">
    </div>
  )
}

Index.defaultProps = {
}

Index.propTypes = {
}

export default Index